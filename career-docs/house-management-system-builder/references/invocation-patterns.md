# 调用示例模板

## 作用

下面这些提示词模板用于让 AI 更稳定地调用本 skill，并按阶段推进房源管理系统开发。

## 从零开始搭建

```text
Use $house-management-system-builder at E:\project\study\all-project\career-docs\house-management-system-builder，根据这个 skill 在当前工作区从零搭建房源管理系统 MVP。先完成前后端项目骨架、登录鉴权、基础布局、请求封装、统一响应结构和具备 RBAC 扩展能力的菜单路由。
```

## 第 1 阶段：鉴权与布局

```text
Use $house-management-system-builder at E:\project\study\all-project\career-docs\house-management-system-builder，根据这个 skill 完成房源管理系统第 1 阶段：项目初始化、登录退出、JWT 鉴权、基于 Redis 的登录态、后台基础布局、动态路由，以及前后端的公共基础设施。
```

## 第 2 阶段：系统管理

```text
Use $house-management-system-builder at E:\project\study\all-project\career-docs\house-management-system-builder，根据这个 skill 完成系统管理阶段，包含员工管理、角色管理、菜单管理，以及按钮级权限控制。
```

## 单模块完整交付

```text
Use $house-management-system-builder at E:\project\study\all-project\career-docs\house-management-system-builder，根据这个 skill 把租客模块作为一个完整纵向切片交付，包含后端接口、前端列表和表单页面、路由入口、权限码，以及证明该模块可用的最小校验。
```

## 房源领域建模与开发

```text
Use $house-management-system-builder at E:\project\study\all-project\career-docs\house-management-system-builder，根据这个 skill 开发房源领域，采用统一的 house 主模型，并使用 house_room 支撑合租与集中式场景。整租、合租、集中管理三个视图要复用同一套后端建模思路。
```

## 首页看板与工单

```text
Use $house-management-system-builder at E:\project\study\all-project\career-docs\house-management-system-builder，根据这个 skill 开发首页看板、维修工单和保洁工单，提供聚合概览接口、趋势接口、适配 ECharts 的前端数据接入，以及带权限控制的页面。
```

## 部署与交付文档

```text
Use $house-management-system-builder at E:\project\study\all-project\career-docs\house-management-system-builder，根据这个 skill 整理 MVP 的部署与交付材料，包含 SQL 或初始化数据说明、接口或模块文档、Docker Compose、Nginx 路由说明，以及适合面试讲解的项目总结。
```
